// const net = require("net");

// const server = net.createServer((socket) => {
//     socket.on("data", (data) => {
//         console.log("Data received via TCP client: ", data.toString());
//     });
//     socket.on('end', () => console.log("TCP Connection ended"));
// });

// server.listen(8080, () => console.log("TCP Sever opened on 8080"));